import './MapExt'

describe('MapExt', () => {
  test('should be able to create instance', () => {
    expect(new Map()).toBeInstanceOf(Map)
  })

  test('should be able to set a item', () => {
    expect(new Map()).toBeInstanceOf(Map)
  })
})
